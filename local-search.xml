<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
  
  <entry>
    <title>Ubuntu 24.04 系统下黑屏的急救与彻底修复</title>
    <link href="/2026/06/01/ubuntu-24.04-hwe-kernel-blackscreen-fix/"/>
    <url>/2026/06/01/ubuntu-24.04-hwe-kernel-blackscreen-fix/</url>
    
    <content type="html"><![CDATA[<h1 id="Linux-相关-Ubuntu-24-04-系统下黑屏的急救与彻底修复"><a href="#Linux-相关-Ubuntu-24-04-系统下黑屏的急救与彻底修复" class="headerlink" title="Linux 相关: Ubuntu 24.04 系统下黑屏的急救与彻底修复"></a>Linux 相关: Ubuntu 24.04 系统下黑屏的急救与彻底修复</h1><blockquote><p><strong>本篇博客的背景</strong>：Ubuntu 24.04 LTS 系统，使用 AMD Ryzen + NVIDIA Optimus 双显卡笔记本（联想拯救者 R7000P 2021H），升级了英伟达驱动后重启黑屏，左上角有闪烁光标。</p></blockquote><h2 id="环境清单"><a href="#环境清单" class="headerlink" title="环境清单"></a>环境清单</h2><table><thead><tr><th>项目</th><th>值</th></tr></thead><tbody><tr><td>OS</td><td>Ubuntu 24.04.3 LTS (Noble)</td></tr><tr><td>内核</td><td>6.17.0-35-generic（HWE）</td></tr><tr><td>GPU</td><td>NVIDIA GeForce RTX 3060 Laptop GPU + AMD Renoir 集显</td></tr><tr><td>显示管理器</td><td>gdm3</td></tr><tr><td>显示服务器</td><td>Xorg</td></tr><tr><td>NVIDIA 驱动</td><td>580.159.03（server LTS 分支）</td></tr><tr><td>Secure Boot</td><td>开启</td></tr></tbody></table><hr><h2 id="1-确认本篇博客是否有可能解决问题"><a href="#1-确认本篇博客是否有可能解决问题" class="headerlink" title="1. 确认本篇博客是否有可能解决问题"></a>1. 确认本篇博客是否有可能解决问题</h2><p>在尝试任何修复之前，请读者先确认自己电脑的症状是否匹配以下情况：</p><ul><li>重启后黑屏，左上角有闪烁光标（也有可能是完全的黑屏，连光标都没有）</li><li>系统最近自动更新过内核（比如升级英伟达驱动导致内核被捎带着一起升级了），在 grub 界面中，输入 <code>ls /boot/vmlinuz-*</code> 能看到多个版本</li><li>能通过 CTRL+ALT+F2（或者 F3~F6）进入 TTY 界面并且登录账号</li></ul><p>如果 TTY 都进不去，那这篇文章有可能无法解决你的电脑故障。</p><hr><h2 id="2-急救方法：先从-GRUB-命令行启动到旧内核"><a href="#2-急救方法：先从-GRUB-命令行启动到旧内核" class="headerlink" title="2. 急救方法：先从 GRUB 命令行启动到旧内核"></a>2. 急救方法：先从 GRUB 命令行启动到旧内核</h2><p>这是首要任务，首先要能成功启动图形化桌面，否则对于不太熟悉命令行界面的读者来说，大部分操作都不方便进行。</p><h3 id="2-1-进入-GRUB-命令行"><a href="#2-1-进入-GRUB-命令行" class="headerlink" title="2.1 进入 GRUB 命令行"></a>2.1 进入 GRUB 命令行</h3><p>重启电脑，在启动时反复按 <code>Esc</code> 或按住 <code>Shift</code> 进入 GRUB 菜单。如果只看到命令行界面（<code>grub&gt;</code>），直接跳到下一步。</p><p>如果看到菜单但找不到 “Advanced options for Ubuntu”：</p><ul><li>有些 GRUB 配置隐藏了菜单，按 <code>Esc</code> 或 <code>e</code> 进入编辑模式</li><li>如果完全卡死，强制关机再开机，反复按 <code>Esc</code></li></ul><h3 id="2-2-探测你的分区位置"><a href="#2-2-探测你的分区位置" class="headerlink" title="2.2 探测你的分区位置"></a>2.2 探测你的分区位置</h3><blockquote><p><strong>提醒：不要照抄以下命令，要看自己电脑的输出内容</strong>：</p></blockquote><p>在 <code>grub&gt;</code> 提示符下，逐条输入以下命令。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span><br></code></pre></td></tr></table></figure><p>你会看到类似 <code>(hd0) (hd0,gpt1) (hd0,gpt2)</code> 的输出。从第一个分区开始排查：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> (hd0,gpt2)/<br></code></pre></td></tr></table></figure><p><strong>找根分区</strong>：如果看到 <code>boot/ etc/ home/ usr/ var/</code> 这些目录，这就是你的根分区。如果只看到 <code>EFI/</code>，那是 ESP 分区，继续排查其他分区，直到找出来哪个是根分区。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> (hd0,gpt2)/boot/<br></code></pre></td></tr></table></figure><p>确认里面有 <code>vmlinuz-&lt;版本号&gt;-generic</code> 和 <code>initrd.img-&lt;版本号&gt;-generic</code> 文件，比如我的是 <code>vmlinuz-6.17.0-29-generic</code> 等文件。</p><p>从<strong>版本号第二大</strong>的内核开始尝试，记住版本号。这么做是因为，通常版本号最大的那个内核就是当前会导致黑屏故障的那个。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">cat</span> (hd0,gpt2)/etc/fstab<br></code></pre></td></tr></table></figure><p>看根挂载点用的标识符，通常是 <code>UUID=xxxx-xxxx</code> 或 <code>/dev/nvme0n1p2</code> 这种设备名，记住它。</p><h3 id="2-3-启动旧内核"><a href="#2-3-启动旧内核" class="headerlink" title="2.3 启动旧内核"></a>2.3 启动旧内核</h3><p>假设根分区是 <code>(hd0,gpt2)</code>，fstab 里根设备是 <code>/dev/nvme0n1p2</code>（一般联想拯救者就是这个设备名了），要启动的内核是 <code>6.17.0-29-generic</code>：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">set</span> root=(hd0,gpt2)<br><span class="hljs-attribute">linux</span> /boot/vmlinuz-<span class="hljs-number">6</span>.<span class="hljs-number">17</span>.<span class="hljs-number">0</span>-<span class="hljs-number">29</span>-generic root=/dev/nvme0n1p2 ro quiet splash<br><span class="hljs-attribute">initrd</span> /boot/initrd.img-<span class="hljs-number">6</span>.<span class="hljs-number">17</span>.<span class="hljs-number">0</span>-<span class="hljs-number">29</span>-generic<br><span class="hljs-attribute">boot</span><br></code></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>：<code>/dev/nvme0n1p2</code> 可能在 GRUB 阶段不存在（那是 Linux 内核启动后才创建的设备节点），但内核启动后能识别。如果你在 GRUB 下 <code>ls /dev/</code> 只找到了一个 <code>/dev/null</code>，这是正常情况。</p></blockquote><p>如果 <code>root=/dev/nvme0n1p2</code> 不工作，换成 fstab 里看到的 UUID 写法：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">linux</span> /boot/vmlinuz-<span class="hljs-number">6</span>.<span class="hljs-number">17</span>.<span class="hljs-number">0</span>-<span class="hljs-number">29</span>-generic root=UUID=&lt;你的UUID&gt; ro quiet splash<br></code></pre></td></tr></table></figure><h3 id="2-4-进入桌面后第一件事"><a href="#2-4-进入桌面后第一件事" class="headerlink" title="2.4 进入桌面后第一件事"></a>2.4 进入桌面后第一件事</h3><p>如果能成功进入图形化桌面了，那请立即打开终端，输入以下内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 让 GRUB 菜单以后都显示出来（避免下次又要手敲）</span><br><span class="hljs-built_in">sudo</span> sed -i <span class="hljs-string">&#x27;s/GRUB_TIMEOUT_STYLE=hidden/GRUB_TIMEOUT_STYLE=menu/&#x27;</span> /etc/default/grub<br><span class="hljs-built_in">sudo</span> sed -i <span class="hljs-string">&#x27;s/GRUB_TIMEOUT=0/GRUB_TIMEOUT=5/&#x27;</span> /etc/default/grub<br><span class="hljs-built_in">sudo</span> update-grub<br></code></pre></td></tr></table></figure><hr><h2 id="3-永久修复（推荐方案，约-5-分钟）"><a href="#3-永久修复（推荐方案，约-5-分钟）" class="headerlink" title="3. 永久修复（推荐方案，约 5 分钟）"></a>3. 永久修复（推荐方案，约 5 分钟）</h2><h3 id="3-1-黑屏的根因"><a href="#3-1-黑屏的根因" class="headerlink" title="3.1 黑屏的根因"></a>3.1 黑屏的根因</h3><p>以下结论来自我自己的电脑，请读者按自己的电脑详细情况调整命令：本机最新的内核版本为 <code>6.17.0-35-generic</code>。但是这个 6.17.0-35 HWE 内核安装时，**<code>linux-modules-extra-6.17.0-35-generic</code> 包没有自动安装。** 这个包里面有 <code>amdgpu</code> 等非核心内核模块。</p><p>对于 R7000P 这个 AMD Ryzen + NVIDIA 双显卡笔记本，显示屏是接在 AMD 集显上的，没有 amdgpu 模块等于屏幕没驱动，屏幕没驱动等于 Xorg 启动失败，然后就是 GDM 反复重启，电脑成功黑屏。</p><p><strong>所以其实也不是 NVIDIA 驱动的问题，是 AMD 集显模块没装上。</strong></p><h3 id="3-2-修复步骤"><a href="#3-2-修复步骤" class="headerlink" title="3.2 修复步骤"></a>3.2 修复步骤</h3><ol><li><p>安装 35 内核缺失的 extras 包和 headers</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt install linux-modules-extra-6.17.0-35-generic linux-headers-6.17.0-35-generic<br></code></pre></td></tr></table></figure></li><li><p>保证固件最新</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt install --reinstall linux-firmware<br></code></pre></td></tr></table></figure></li><li><p>验证 amdgpu 模块已安装</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">ls</span> /lib/modules/6.17.0-35-generic/kernel/drivers/gpu/drm/amd/amdgpu/amdgpu.ko*<br></code></pre></td></tr></table></figure><p>应该能看到 amdgpu.ko.zst 或者 amdgpu.ko</p></li><li><p>重建 <code>6.17.0-35-generic</code> 内核的 initramfs</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> update-initramfs -u -k 6.17.0-35-generic<br></code></pre></td></tr></table></figure></li><li><p>重启</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> reboot<br></code></pre></td></tr></table></figure></li></ol><h3 id="3-3-验证修复"><a href="#3-3-验证修复" class="headerlink" title="3.3 验证修复"></a>3.3 验证修复</h3><p>在 3.2 步重启后，如果桌面能正常显示，那么可以开始确认以下内容：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">uname</span> -r                    <span class="hljs-comment"># 对于我自己的电脑，应输出 6.17.0-35-generic</span><br><span class="hljs-built_in">ls</span> /dev/dri/                <span class="hljs-comment"># 对于我自己的电脑，输出为: card1 card2 renderD128 renderD129</span><br>lsmod | grep amdgpu         <span class="hljs-comment"># 应有 amdgpu 模块</span><br>nvidia-smi                  <span class="hljs-comment"># 应正常输出 GPU 信息</span><br></code></pre></td></tr></table></figure><p>如果输出符合预期，修复就算完成了。</p><h3 id="3-4-防止黑屏可做的进一步操作"><a href="#3-4-防止黑屏可做的进一步操作" class="headerlink" title="3.4 防止黑屏可做的进一步操作"></a>3.4 防止黑屏可做的进一步操作</h3><blockquote><p><strong>提醒</strong>：以下操作不一定真的管用，如果你的电脑能跑，那就没必要再费心了</p></blockquote><p>每次 linux 内核升级都有可能缺 extras 包。为了防止旧病复发，可以尝试以下操作：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 方案 A：锁定 HWE meta 包（阻止自动拉新内核）</span><br><span class="hljs-built_in">sudo</span> apt-mark hold linux-generic-hwe-24.04 linux-headers-generic-hwe-24.04 linux-image-generic-hwe-24.04<br><br><span class="hljs-comment"># 方案 B：让 GRUB 默认启动用名字锁定（菜单顺序变了也不会指错）</span><br><span class="hljs-built_in">sudo</span> sed -i <span class="hljs-string">&#x27;s|^GRUB_DEFAULT=.*|GRUB_DEFAULT=&quot;Advanced options for Ubuntu&gt;Ubuntu, with Linux 6.17.0-35-generic&quot;|&#x27;</span> /etc/default/grub<br><span class="hljs-built_in">sudo</span> update-grub<br><br><span class="hljs-comment"># 方案 C：关闭无人值守自动升级，可选，不推荐长期关闭，除非你会手动更新</span><br><span class="hljs-built_in">sudo</span> sed -i <span class="hljs-string">&#x27;s|^APT::Periodic::Unattended-Upgrade.*|APT::Periodic::Unattended-Upgrade &quot;0&quot;;|&#x27;</span> /etc/apt/apt.conf.d/20auto-upgrades<br></code></pre></td></tr></table></figure><blockquote><p><strong>注意</strong>：你可能之前在网上搜索教程，并运行过 <code>apt-mark hold linux-image-generic linux-headers-generic</code>，但是这条命令 <strong>对HWE 内核无效</strong>。6.17 是 HWE 内核，由 <code>linux-generic-hwe-24.04</code> 这个 meta 包管理，不是 <code>linux-image-generic</code>。先 <code>sudo apt-mark unhold linux-image-generic linux-headers-generic</code> 解掉，再 hold 上面正确的包。</p></blockquote><h3 id="3-5-以后手动升级内核的正确方法"><a href="#3-5-以后手动升级内核的正确方法" class="headerlink" title="3.5 以后手动升级内核的正确方法"></a>3.5 以后手动升级内核的正确方法</h3><p>当你做完了 3.4 的方案 A，那么以后升级到新内核的时候，就可以按下面的操作来：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 先解 hold</span><br><span class="hljs-built_in">sudo</span> apt-mark unhold linux-generic-hwe-24.04 linux-headers-generic-hwe-24.04 linux-image-generic-hwe-24.04<br><br><span class="hljs-comment"># 升级</span><br><span class="hljs-built_in">sudo</span> apt update &amp;&amp; <span class="hljs-built_in">sudo</span> apt upgrade<br><br><span class="hljs-comment"># 立刻装 extras（关键！）</span><br><span class="hljs-built_in">sudo</span> apt install linux-modules-extra-$(<span class="hljs-built_in">uname</span> -r) linux-headers-$(<span class="hljs-built_in">uname</span> -r)<br><br><span class="hljs-comment"># 验证 amdgpu 存在</span><br><span class="hljs-built_in">ls</span> /lib/modules/$(<span class="hljs-built_in">uname</span> -r)/kernel/drivers/gpu/drm/amd/amdgpu/amdgpu.ko*<br><br><span class="hljs-comment"># 重新锁住</span><br><span class="hljs-built_in">sudo</span> apt-mark hold linux-generic-hwe-24.04 linux-headers-generic-hwe-24.04 linux-image-generic-hwe-24.04<br></code></pre></td></tr></table></figure><hr><h2 id="4-完整排查复盘（可选阅读）"><a href="#4-完整排查复盘（可选阅读）" class="headerlink" title="4. 完整排查复盘（可选阅读）"></a>4. 完整排查复盘（可选阅读）</h2><p>以下是我自己记录的完整排查过程。如果读者对”为什么”感兴趣，或者电脑状况跟上面的不完全匹配，可以对照排查一下。</p><h3 id="4-1-第一层：GDM-服务状态"><a href="#4-1-第一层：GDM-服务状态" class="headerlink" title="4.1 第一层：GDM 服务状态"></a>4.1 第一层：GDM 服务状态</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><code class="hljs bash">systemctl status gdm3<br><span class="hljs-comment"># 显示 active(running)，但有报错：</span><br><span class="hljs-comment"># Gdm: on_display_removed: assertion &#x27;GDM_IS_REMOTE_DISPLAY (display)&#x27; failed</span><br><br>loginctl<br><span class="hljs-comment"># 只列出 tty session，没有图形化 session</span><br><span class="hljs-comment"># 这导致 GDM 虽然是 running 状态，但是没能成功拉起图形界面</span><br></code></pre></td></tr></table></figure><p>同时发现 <code>/etc/gdm3/custom.conf</code> 中 <code>AutomaticLogin = user1</code> 指向不存在的用户，这会导致 GDM 启动时卡住。修复：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> sed -i <span class="hljs-string">&#x27;s/AutomaticLogin = user1/AutomaticLogin = &lt;我的用户名&gt;/&#x27;</span> /etc/gdm3/custom.conf<br></code></pre></td></tr></table></figure><h3 id="4-2-第二层：DKMS-与-NVIDIA-驱动"><a href="#4-2-第二层：DKMS-与-NVIDIA-驱动" class="headerlink" title="4.2 第二层：DKMS 与 NVIDIA 驱动"></a>4.2 第二层：DKMS 与 NVIDIA 驱动</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash">dkms status<br><span class="hljs-comment"># nvidia-srv/580.159.03, 6.17.0-35-generic: installed (WARNING! Diff between built and installed module!)</span><br><span class="hljs-comment"># DKMS 编译的模块与已安装模块不一致</span><br></code></pre></td></tr></table></figure><p><code>dkms autoinstall</code> 报错：35 内核缺少 headers。从这里就能看出 35 内核的安装是不完整的。</p><blockquote><p>以下操作是我排查过程中走的弯路，<strong>仅作记录，请勿执行</strong>。</p><ul><li>进图形化桌面后就执行 <code>sudo ubuntu-drivers autoinstall</code>——这导致了安装 <code>nvidia-driver-595-open</code> 驱动，并且我们又和没安装完整的 35 内核见面了。595-open 也与当前系统不兼容，导致黑屏更严重，状况直接从左上角闪光标变成了完全的黑屏</li><li><code>sudo apt-mark hold linux-image-generic linux-headers-generic</code>——对 HWE 内核无效，锁错了 meta 包</li><li><code>GRUB_DEFAULT=&quot;5&gt;4&quot;</code>——grub 启动的数字索引写错了一次，然后启动的时候找不到对应内核，就又退回了 35 内核了（我们又见面啦）</li></ul></blockquote><h3 id="4-3-第三层：NVIDIA-用户态库版本污染"><a href="#4-3-第三层：NVIDIA-用户态库版本污染" class="headerlink" title="4.3 第三层：NVIDIA 用户态库版本污染"></a>4.3 第三层：NVIDIA 用户态库版本污染</h3><p>以下文件名是我自己电脑上的，随着时间推移导致内容过期，读者可能要自行搜索适配的驱动版本：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">dpkg -S $(<span class="hljs-built_in">which</span> nvidia-persistenced)<br><span class="hljs-comment"># nvidia-compute-utils-580-server</span><br><br>nvidia-persistenced --version<br><span class="hljs-comment"># version 590.48.01</span><br><span class="hljs-comment"># 包管理器认为是 580，实际磁盘上是 590 残留</span><br></code></pre></td></tr></table></figure><p>我之前执行过一次 <code>apt purge</code>，但是东西没清干净。磁盘上残留了 44 个 590 版本的 <code>.so</code> 文件和 6 个 590 版本的二进制文件，导致内核模块（580）与用户态工具（590）API 不匹配。</p><p>修复：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 删除所有 590 残留</span><br>find /usr/lib /usr/lib32 -name <span class="hljs-string">&#x27;*nvidia*590*&#x27;</span> -delete<br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> /usr/bin/nvidia-persistenced /usr/bin/nvidia-powerd \<br>        /usr/bin/nvidia-pcc /usr/bin/nvidia-ngx-updater \<br>        /usr/bin/nvidia-cuda-mps-control /usr/bin/nvidia-cuda-mps-server<br><br><span class="hljs-comment"># 强制重装 580 包</span><br><span class="hljs-built_in">sudo</span> apt install --reinstall nvidia-compute-utils-580-server nvidia-utils-580-server \<br>                             libnvidia-compute-580-server libnvidia-decode-580-server \<br>                             libnvidia-encode-580-server libnvidia-fbc1-580-server \<br>                             libnvidia-gl-580-server libnvidia-cfg1-580-server<br></code></pre></td></tr></table></figure><h3 id="4-4-第四层：modprobe-配置错误"><a href="#4-4-第四层：modprobe-配置错误" class="headerlink" title="4.4 第四层：modprobe 配置错误"></a>4.4 第四层：modprobe 配置错误</h3><p>这里是最稀奇的地方，不知道有没有读者和我有相似的问题：<code>/lib/modprobe.d/nvidia-kms.conf</code> 有两行被错误拼接：</p><figure class="highlight apache"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs apache"><span class="hljs-attribute">options</span> nvidia-drm modeset=<span class="hljs-number">1</span>options nvidia <span class="hljs-string">&quot;NVreg_DynamicPowerManagement=0x02&quot;</span><br></code></pre></td></tr></table></figure><p><code>modeset=1options</code> 不是有效参数。修复：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">tee</span> /lib/modprobe.d/nvidia-kms.conf &lt;&lt;<span class="hljs-string">&#x27;EOF&#x27;</span><br>options nvidia-drm modeset=1<br>EOF<br><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">rm</span> /lib/modprobe.d/nvidia-runtimepm.conf  <span class="hljs-comment"># 激进 runtime PM 导致 GPU 冷启动问题</span><br></code></pre></td></tr></table></figure><h3 id="4-5-第五层：Secure-Boot-误判"><a href="#4-5-第五层：Secure-Boot-误判" class="headerlink" title="4.5 第五层：Secure Boot 误判"></a>4.5 第五层：Secure Boot 误判</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><code class="hljs bash">mokutil --sb-state<br><span class="hljs-comment"># SecureBoot enabled</span><br><br>modinfo nvidia.ko | grep signer<br><span class="hljs-comment"># signer: Canonical Ltd. Kernel Module Signing</span><br><span class="hljs-comment"># 签名链完整有效，不是 Secure Boot 问题</span><br></code></pre></td></tr></table></figure><p>判断标准：如果日志中出现 <code>module verification failed: signature and/or required key missing</code> 才是 Secure Boot 签名问题。看到 <code>loading out-of-tree module taints kernel</code> 说明模块加载成功，签名 OK。</p><h3 id="4-6-第六层：-etc-X11-xorg-conf-遗留"><a href="#4-6-第六层：-etc-X11-xorg-conf-遗留" class="headerlink" title="4.6 第六层：&#x2F;etc&#x2F;X11&#x2F;xorg.conf 遗留"></a>4.6 第六层：&#x2F;etc&#x2F;X11&#x2F;xorg.conf 遗留</h3><p>可能是之前某次 <code>nvidia-xconfig</code> 生成了 <code>/etc/X11/xorg.conf</code> 文件，里面写死了 nvidia 作为唯一显示设备。在双显卡笔记本上，这个东西阻止了 Xorg 使用 PRIME 路径（amdgpu 显示 + nvidia 渲染）。</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> <span class="hljs-built_in">mv</span> /etc/X11/xorg.conf /etc/X11/xorg.conf.bak<br><span class="hljs-built_in">sudo</span> apt install nvidia-prime<br><span class="hljs-built_in">sudo</span> prime-select on-demand<br></code></pre></td></tr></table></figure><h3 id="4-7-第七层（根因）：amdgpu-模块缺失"><a href="#4-7-第七层（根因）：amdgpu-模块缺失" class="headerlink" title="4.7 第七层（根因）：amdgpu 模块缺失"></a>4.7 第七层（根因）：amdgpu 模块缺失</h3><p>35 内核的启动日志里面完全没有 amdgpu 加载记录：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 35 内核（失败）</span><br>grep <span class="hljs-string">&#x27;amdgpu&#x27;</span> ——只有 CPU 名称 <span class="hljs-string">&quot;AMD Ryzen 7 5800H with Radeon Graphics&quot;</span><br><span class="hljs-built_in">ls</span> /dev/dri/ ——card1 card2（只有 nvidia，没有 amdgpu 的 card0）<br><br><span class="hljs-comment"># 29 内核（成功）</span><br>kernel: [drm] amdgpu kernel modesetting enabled.<br>kernel: [drm] Detected VRAM RAM=4096M<br><span class="hljs-built_in">ls</span> /dev/dri/ → card1 card2 renderD128 renderD129<br></code></pre></td></tr></table></figure><p>排查到 Xorg 日志，里面的内容也能和上面的记录进行交叉验证：</p><figure class="highlight stata"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><code class="hljs stata">(EE) systemd-logind: failed to take device /dev/dri/card0: <span class="hljs-keyword">No</span> such <span class="hljs-keyword">file</span> or directory<br>(EE) <span class="hljs-keyword">open</span> /dev/dri/card0: <span class="hljs-keyword">No</span> such <span class="hljs-keyword">file</span> or directory<br>(EE) <span class="hljs-keyword">No</span> devices detected.<br>Fatal server <span class="hljs-keyword">error</span>:<br></code></pre></td></tr></table></figure><p><code>/dev/dri/card0</code> 是 amdgpu 的 DRM 节点，它不存在，那 Xorg 就找不到任何可用显示设备，GDM 也就跑不起来了。</p><p>最终修复就是本文第 3 节的内容：安装 <code>linux-modules-extra-6.17.0-35-generic</code>。</p><hr><h2 id="5-防黑屏脚本"><a href="#5-防黑屏脚本" class="headerlink" title="5. 防黑屏脚本"></a>5. 防黑屏脚本</h2><p>以下脚本读者可以复制粘贴到终端运行，检查以下自己的系统是否有类似隐患：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-meta">#!/bin/bash</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== 内核版本 ===&quot;</span><br><span class="hljs-built_in">uname</span> -r<br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== 当前内核 extras 包状态 ===&quot;</span><br>dpkg -l <span class="hljs-string">&quot;linux-modules-extra-<span class="hljs-subst">$(uname -r)</span>&quot;</span> 2&gt;/dev/null | <span class="hljs-built_in">tail</span> -1<br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== amdgpu 模块是否存在 ===&quot;</span><br><span class="hljs-built_in">ls</span> /lib/modules/$(<span class="hljs-built_in">uname</span> -r)/kernel/drivers/gpu/drm/amd/amdgpu/amdgpu.ko* 2&gt;/dev/null || <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;缺失！需要安装 linux-modules-extra-<span class="hljs-subst">$(uname -r)</span>&quot;</span><br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== NVIDIA 用户态/内核模块版本一致性 ===&quot;</span><br>KERNEL_VER=$(modinfo nvidia 2&gt;/dev/null | grep <span class="hljs-string">&#x27;^version&#x27;</span> | awk <span class="hljs-string">&#x27;&#123;print $2&#125;&#x27;</span>)<br>USER_VER=$(nvidia-smi --version 2&gt;/dev/null | grep <span class="hljs-string">&#x27;NVIDIA-SMI version&#x27;</span> | awk <span class="hljs-string">&#x27;&#123;print $4&#125;&#x27;</span>)<br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;内核模块: <span class="hljs-variable">$KERNEL_VER</span>&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;用户态:   <span class="hljs-variable">$USER_VER</span>&quot;</span><br><span class="hljs-keyword">if</span> [ <span class="hljs-string">&quot;<span class="hljs-variable">$KERNEL_VER</span>&quot;</span> != <span class="hljs-string">&quot;<span class="hljs-variable">$USER_VER</span>&quot;</span> ] &amp;&amp; [ -n <span class="hljs-string">&quot;<span class="hljs-variable">$KERNEL_VER</span>&quot;</span> ]; <span class="hljs-keyword">then</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;⚠️ 版本不匹配！&quot;</span><br><span class="hljs-keyword">fi</span><br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== xorg.conf 遗留检查 ===&quot;</span><br><span class="hljs-keyword">if</span> [ -f /etc/X11/xorg.conf ]; <span class="hljs-keyword">then</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;⚠️ /etc/X11/xorg.conf 存在，可能阻止 PRIME 正常工作&quot;</span><br><span class="hljs-keyword">else</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;无遗留 xorg.conf&quot;</span><br><span class="hljs-keyword">fi</span><br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== PRIME 模式 ===&quot;</span><br>prime-select query 2&gt;/dev/null || <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;nvidia-prime 未安装&quot;</span><br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== NVIDIA 孤儿库残留检查 ===&quot;</span><br>ORPHANS=$(find /usr/lib -name <span class="hljs-string">&#x27;*nvidia*&#x27;</span> ! -path <span class="hljs-string">&#x27;*/modules/*&#x27;</span> -<span class="hljs-built_in">exec</span> sh -c <span class="hljs-string">&#x27;dpkg -S &quot;$1&quot; &gt;/dev/null 2&gt;&amp;1 || echo &quot;$1&quot;&#x27;</span> _ &#123;&#125; \; 2&gt;/dev/null | <span class="hljs-built_in">head</span> -5)<br><span class="hljs-keyword">if</span> [ -n <span class="hljs-string">&quot;<span class="hljs-variable">$ORPHANS</span>&quot;</span> ]; <span class="hljs-keyword">then</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;⚠️ 发现孤儿文件（不属于任何已安装包）：&quot;</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;<span class="hljs-variable">$ORPHANS</span>&quot;</span><br><span class="hljs-keyword">else</span><br>    <span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;无孤儿文件&quot;</span><br><span class="hljs-keyword">fi</span><br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== GRUB 默认启动项 ===&quot;</span><br>grep GRUB_DEFAULT /etc/default/grub<br><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;&quot;</span><br><span class="hljs-built_in">echo</span> <span class="hljs-string">&quot;=== 可用内核列表 ===&quot;</span><br><span class="hljs-built_in">ls</span> /boot/vmlinuz-* | sed <span class="hljs-string">&#x27;s/.*vmlinuz-/  /&#x27;</span><br></code></pre></td></tr></table></figure><hr><h2 id="6-FAQ"><a href="#6-FAQ" class="headerlink" title="6. FAQ"></a>6. FAQ</h2><h3 id="Q-需要关-Secure-Boot-吗？"><a href="#Q-需要关-Secure-Boot-吗？" class="headerlink" title="Q: 需要关 Secure Boot 吗？"></a>Q: 需要关 Secure Boot 吗？</h3><p><strong>不需要。</strong> 本文场景中 Secure Boot 不是根因。Canonical 签名的 NVIDIA 模块在 Secure Boot 开启时正常工作。只有当日志中出现 <code>module verification failed</code> 时才是 Secure Boot 问题。</p><h3 id="Q-能不能直接退回旧版本内核，长期不升级？"><a href="#Q-能不能直接退回旧版本内核，长期不升级？" class="headerlink" title="Q: 能不能直接退回旧版本内核，长期不升级？"></a>Q: 能不能直接退回旧版本内核，长期不升级？</h3><p>可以，但不推荐把这个当成最终的解决方法。</p><p>如果只是想先把桌面抢救回来，回退旧内核完全合理。本文第 2 节的 GRUB 手动启动目的就是这个。不过换用旧内核会带来几个长期隐患：</p><ol><li>安全更新可能跟不上</li><li>新硬件、驱动、固件适配可能出问题</li><li>以后安装 NVIDIA、VirtualBox、Docker、v4l2loopback 等依赖内核模块的东西时，仍然可能遇到 DKMS、headers、modules-extra 不匹配的问题</li><li>Ubuntu 的 HWE 内核本来就是给较新硬件准备的，长期停在旧内核可能只是把问题往后推，然后在未来炸飞自己</li></ol><p>如果读者明确自己要这样做，可以按下面的方式把“能启动的旧内核”固定为默认启动项：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-comment"># 例：固定启动 6.17.0-29-generic，按读者电脑上的实际版本修改</span><br><span class="hljs-built_in">sudo</span> sed -i <span class="hljs-string">&#x27;s|^GRUB_DEFAULT=.*|GRUB_DEFAULT=&quot;Advanced options for Ubuntu&gt;Ubuntu, with Linux 6.17.0-29-generic&quot;|&#x27;</span> /etc/default/grub<br><span class="hljs-built_in">sudo</span> update-grub<br></code></pre></td></tr></table></figure><p>然后把它标记为手动安装，避免被 <code>autoremove</code> 删除：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-mark manual \<br>  linux-image-6.17.0-29-generic \<br>  linux-headers-6.17.0-29-generic \<br>  linux-modules-6.17.0-29-generic \<br>  linux-modules-extra-6.17.0-29-generic<br></code></pre></td></tr></table></figure><p>如果还想阻止 HWE 自动拉新内核：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-mark hold linux-generic-hwe-24.04 linux-headers-generic-hwe-24.04 linux-image-generic-hwe-24.04<br></code></pre></td></tr></table></figure><p>以后想恢复自动升级：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><code class="hljs bash"><span class="hljs-built_in">sudo</span> apt-mark unhold linux-generic-hwe-24.04 linux-headers-generic-hwe-24.04 linux-image-generic-hwe-24.04<br></code></pre></td></tr></table></figure><p>旧内核仅适合急救和备用，不适合作为长期方案。我认为最好还是保留一个能启动的旧内核，然后把新内核缺失的 <code>linux-modules-extra</code>、headers 和 NVIDIA 模块补齐。</p><h3 id="Q-Wayland-还是-Xorg？"><a href="#Q-Wayland-还是-Xorg？" class="headerlink" title="Q: Wayland 还是 Xorg？"></a>Q: Wayland 还是 Xorg？</h3><p>本文场景使用 Xorg。如果你用 Wayland，<code>/etc/gdm3/custom.conf</code> 中 <code>WaylandEnable=false</code> 那行改成 <code>true</code> 即可。但 NVIDIA + Wayland 在 24.04 上仍有一些已知问题，建议先用 Xorg 稳定后再试。</p><h3 id="Q-双系统-Windows-有影响吗？"><a href="#Q-双系统-Windows-有影响吗？" class="headerlink" title="Q: 双系统 Windows 有影响吗？"></a>Q: 双系统 Windows 有影响吗？</h3><p>没有。GRUB 和内核选择与 Windows 无关。</p><h3 id="Q-grub-reboot-是什么？"><a href="#Q-grub-reboot-是什么？" class="headerlink" title="Q: grub-reboot 是什么？"></a>Q: <code>grub-reboot</code> 是什么？</h3><p><code>sudo grub-reboot &quot;菜单项名称&quot;</code> 让 GRUB <strong>仅下一次启动</strong>使用指定内核。如果启动失败，第三次启动会自动回到默认内核。这是测试新内核的一种低风险方法。不过对于本文的情况，grub-reboot 不好用，因为电脑没启动失败，只是 gdm 没拉起图形化桌面。这样的话不论重启多少次都会从指定内核开机，只能通过 grub 再退回默认内核。</p><hr><h2 id="7-排查过程中使用的一些命令"><a href="#7-排查过程中使用的一些命令" class="headerlink" title="7. 排查过程中使用的一些命令"></a>7. 排查过程中使用的一些命令</h2><table><thead><tr><th>场景</th><th>命令</th></tr></thead><tbody><tr><td>GRUB 探测分区</td><td><code>ls</code> &#x2F; <code>ls (hd0,gpt2)/</code></td></tr><tr><td>GRUB 手动启动</td><td><code>linux /boot/vmlinuz-... root=...</code> + <code>initrd ...</code> + <code>boot</code></td></tr><tr><td>看内核列表</td><td><code>ls /boot/vmlinuz-*</code></td></tr><tr><td>看当前内核</td><td><code>uname -r</code></td></tr><tr><td>看 NVIDIA 状态</td><td><code>nvidia-smi</code></td></tr><tr><td>看 DKMS 状态</td><td><code>dkms status</code></td></tr><tr><td>看 DRM 设备</td><td><code>ls /dev/dri/</code></td></tr><tr><td>看已加载模块</td><td><code>lsmod | grep -E &#39;nvidia|amdgpu&#39;</code></td></tr><tr><td>临时指定下次启动内核（本文场景慎用）</td><td><code>sudo grub-reboot &quot;Advanced options for Ubuntu&gt;Ubuntu, with Linux X.X.X-XX-generic&quot;</code></td></tr><tr><td>锁定 HWE 内核</td><td><code>sudo apt-mark hold linux-generic-hwe-24.04</code></td></tr><tr><td>查看锁定列表</td><td><code>apt-mark showhold</code></td></tr><tr><td>安装缺失 extras</td><td><code>sudo apt install linux-modules-extra-$(uname -r)</code></td></tr><tr><td>TTY 联网 (WiFi)</td><td><code>nmcli device wifi connect &quot;SSID&quot; password &quot;密码&quot;</code></td></tr><tr><td>TTY 联网 (有线)</td><td><code>sudo dhclient &lt;网卡名&gt;</code></td></tr><tr><td>看启动日志</td><td><code>sudo journalctl -b -1 -p err --no-pager</code></td></tr><tr><td>看内核日志</td><td><code>sudo journalctl -b -1 -k --no-pager</code></td></tr></tbody></table>]]></content>
    
    
    <categories>
      
      <category>Linux</category>
      
      <category>Ubuntu</category>
      
    </categories>
    
    
    <tags>
      
      <tag>Ubuntu</tag>
      
      <tag>NVIDIA</tag>
      
      <tag>AMD</tag>
      
      <tag>HWE</tag>
      
      <tag>GRUB</tag>
      
      <tag>GDM</tag>
      
    </tags>
    
  </entry>
  
  
  
  
</search>
